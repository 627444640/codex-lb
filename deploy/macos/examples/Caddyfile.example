# Synthetic localhost-only example; edit only the private runtime copy.
{
    admin off
    auto_https disable_redirects
    skip_install_trust
}

https://localhost:8443 {
    bind 127.0.0.1 ::1
    tls internal
    reverse_proxy 127.0.0.1:2455
}
